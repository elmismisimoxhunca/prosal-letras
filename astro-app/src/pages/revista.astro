---
import { getPosts } from '../lib/ghost';
import Layout from '../layouts/Layout.astro';

const allPosts = await getPosts();
const revistas = allPosts.filter(p => p.tags?.some(t => t.slug === 'hash-revista'));
---

<Layout title="Revista PROSAL — PROSAL">
  <main>
    <section class="page-hero">
      <div class="container">
        <h1 class="serif">Revista PROSAL</h1>
        <div class="divider"></div>
        <p class="page-subtitle">Publicación literaria de la Región de Valparaíso. Descarga nuestras ediciones y descubre las voces que construyen la cultura local.</p>
      </div>
    </section>

    <section class="revista-section">
      <div class="container">
        {revistas.length > 0 ? (
          <div class="revista-grid">
            {revistas.map((rev, i) => (
              <article class="revista-card">
                {rev.feature_image ? (
                  <div class="revista-cover">
                    <img src={rev.feature_image} alt={rev.title} />
                  </div>
                ) : (
                  <div class="revista-cover revista-cover-placeholder">
                    <span class="revista-num serif">{String(i + 1).padStart(2, '0')}</span>
                  </div>
                )}
                <div class="revista-info">
                  <h2>{rev.title}</h2>
                  <p class="revista-excerpt">{rev.excerpt}</p>
                  <div class="revista-meta">
                    <time datetime={rev.published_at}>
                      {new Date(rev.published_at).toLocaleDateString('es-CL', {
                        year: 'numeric',
                        month: 'long',
                      })}
                    </time>
                  </div>
                  <a href={`/blog/${rev.slug}`} class="btn-revista">Leer edición →</a>
                </div>
              </article>
            ))}
          </div>
        ) : (
          <div class="coming-soon">
            <div class="coming-soon-cover">
              <span class="coming-soon-num serif">01</span>
            </div>
            <div class="coming-soon-info">
              <h2 class="serif">Primera edición en preparación</h2>
              <p>Estamos trabajando en la primera edición de la Revista PROSAL, una publicación que reunirá columnas, entrevistas, reseñas y creaciones literarias de autores y autoras de la Región de Valparaíso.</p>
              <p>Síguenos en <a href="https://instagram.com/prosal.letras" target="_blank" rel="noopener">@prosal.letras</a> para enterarte cuando esté disponible.</p>
            </div>
          </div>
        )}
      </div>
    </section>

    <section class="revista-about">
      <div class="container">
        <div class="about-grid">
          <div class="about-text">
            <h2 class="serif">Sobre la Revista</h2>
            <p>La Revista PROSAL es una publicación literaria y cultural que nace desde la Región de Valparaíso con el objetivo de visibilizar y amplificar las voces autorales del territorio.</p>
            <p>Cada edición reúne columnas de opinión, entrevistas a autores, reseñas de libros, creaciones literarias y reflexiones sobre el ecosistema cultural regional.</p>
            <p>La revista es de acceso libre y gratuito, en coherencia con nuestro principio de democratización de la información.</p>
          </div>
          <div class="about-cta">
            <h3>¿Quieres colaborar?</h3>
            <p>Si eres autor/a de la Región de Valparaíso y te gustaría publicar en la Revista PROSAL, escríbenos.</p>
            <a href="mailto:prosal@contacto.cl" class="btn-contact">Enviar propuesta</a>
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  .page-hero {
    background: #1a1a1a;
    color: #fff;
    padding: 6rem 0 4rem;
    text-align: center;
  }

  .page-hero h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .page-hero .divider {
    background: #fff;
    width: 60px;
    height: 3px;
    margin: 1rem auto;
  }

  .page-subtitle {
    font-size: 1.1rem;
    color: #ccc;
    max-width: 650px;
    margin: 0 auto;
    line-height: 1.7;
  }

  /* Editions listing */
  .revista-section {
    padding: 4rem 0;
  }

  .revista-grid {
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  .revista-card {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 3rem;
    padding: 2.5rem;
    border: 1px solid #e5e5e5;
    transition: transform 0.3s, box-shadow 0.3s;
  }

  .revista-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0,0,0,0.06);
  }

  .revista-cover {
    width: 280px;
    height: 380px;
    overflow: hidden;
    border: 1px solid #e5e5e5;
  }

  .revista-cover img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .revista-cover-placeholder {
    background: #1a1a1a;
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    border: none;
  }

  .revista-num {
    font-size: 5rem;
    font-weight: 700;
    opacity: 0.2;
  }

  .revista-info {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .revista-info h2 {
    font-size: 1.8rem;
    font-weight: 700;
    margin-bottom: 1rem;
    line-height: 1.3;
  }

  .revista-excerpt {
    color: #555;
    font-size: 1rem;
    line-height: 1.8;
    margin-bottom: 1.25rem;
  }

  .revista-meta {
    font-size: 0.8rem;
    color: #999;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 1.5rem;
  }

  .btn-revista {
    display: inline-block;
    padding: 0.8rem 2rem;
    background: #1a1a1a;
    color: #fff;
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-decoration: none;
    transition: background 0.3s;
    align-self: flex-start;
  }

  .btn-revista:hover {
    background: #333;
    opacity: 1;
  }

  /* Coming soon state */
  .coming-soon {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 3rem;
    padding: 3rem;
    border: 2px dashed #e5e5e5;
    align-items: center;
  }

  .coming-soon-cover {
    width: 280px;
    height: 380px;
    background: #1a1a1a;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .coming-soon-num {
    font-size: 5rem;
    font-weight: 700;
    color: #fff;
    opacity: 0.15;
  }

  .coming-soon-info h2 {
    font-size: 1.8rem;
    margin-bottom: 1.25rem;
  }

  .coming-soon-info p {
    color: #666;
    font-size: 1rem;
    line-height: 1.8;
    margin-bottom: 1rem;
  }

  .coming-soon-info a {
    font-weight: 600;
    text-decoration: underline;
  }

  /* About section */
  .revista-about {
    background: #f7f7f7;
    padding: 4rem 0 5rem;
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .about-text h2 {
    font-size: 2rem;
    margin-bottom: 1.25rem;
  }

  .about-text p {
    color: #555;
    font-size: 0.95rem;
    line-height: 1.8;
    margin-bottom: 1rem;
  }

  .about-cta {
    background: #fff;
    padding: 2.5rem;
    border: 1px solid #e5e5e5;
  }

  .about-cta h3 {
    font-size: 1.2rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
  }

  .about-cta p {
    color: #666;
    font-size: 0.9rem;
    line-height: 1.7;
    margin-bottom: 1.5rem;
  }

  .btn-contact {
    display: inline-block;
    padding: 0.8rem 2rem;
    background: #1a1a1a;
    color: #fff;
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-decoration: none;
    transition: background 0.3s;
  }

  .btn-contact:hover {
    background: #333;
    opacity: 1;
  }

  @media (max-width: 768px) {
    .revista-card {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .revista-cover {
      width: 100%;
      height: 300px;
    }

    .coming-soon {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .coming-soon-cover {
      width: 100%;
      height: 250px;
    }

    .about-grid {
      grid-template-columns: 1fr;
    }

    .page-hero h1 {
      font-size: 2.2rem;
    }
  }
</style>
